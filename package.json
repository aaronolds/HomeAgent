{
	"name": "homeagent",
	"private": true,
	"type": "module",
	"engines": {
		"node": ">=22.0.0"
	},
	"scripts": {
		"dev": "pnpm --filter @homeagent/gateway dev",
		"build": "pnpm -r --if-present build",
		"test": "vitest run",
		"lint": "biome check ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"typescript": "^5.8.0",
		"vitest": "^3.0.0"
	}
}
